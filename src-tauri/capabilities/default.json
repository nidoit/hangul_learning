{
  "identifier": "default",
  "description": "Default capability for DocConvert",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "opener:default"
  ]
}
